FROM node:18-alpine

WORKDIR /app

# Copy package files and app code
COPY package*.json ./
COPY server.js ./
COPY src/services ./src/services/

# Install dependencies
RUN npm ci --only=production

# Expose monitor port
EXPOSE 3000

# Start the monitor
CMD ["node", "server.js"] 